{
  "name": "wordsmith",
  "version": "0.1.0",
  "private": true,
  "description": "Learn vocabulary by typing it — SRS scheduling driven by measured recall.",
  "workspaces": [
    "packages/core",
    "packages/api",
    "packages/web",
    "packages/scripts"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace packages/web",
    "test:core": "npm run test --workspace packages/core"
  }
}